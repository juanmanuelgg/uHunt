using namespace std;
#include<cstdio>
#include<iostream>
#include<string>

bool test=true;

int main()
{
    if(test) freopen("../../data/easy/CelebrityJeopardy.in","r",stdin);

    char c;
    while(scanf("%c",&c)!=EOF) printf("%c",c);

    //char equation[100];
    //while(gets(equation)) puts(equation);

    //string str;
    //while(getline(cin,str)) cout<<str<<endl;

    return 0;
}
